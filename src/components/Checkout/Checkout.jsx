import styles from './styles.module.scss';

const Checkout = () => {

    return (
        <section className={styles['checkout']}>
            <h1>
                CHEKOUT
            </h1>
        </section>
    )
}

export default Checkout;